public class Administrator extends User { // Potentially useful if I choose to implement 3.6
    public Administrator(int id, String name, String email, String password, Role role) {
        super(id, name, email, password, role);
    }
}